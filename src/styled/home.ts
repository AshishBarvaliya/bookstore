import styled from '@emotion/styled'

export const StyledContainer = styled.div`
  background-color: #f3f2ed;

  .Banner {
    position: relative;
    box-shadow: none;
    background-color: #f3f2ed;

    .BannerFlex {
      display: flex;
      height: 100%;
      position: relative;
    }
  }
`
